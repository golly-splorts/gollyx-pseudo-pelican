#!/bin/bash

export GOLLYX_PSEUDO_PELICAN_HOME="${HOME}/golly/gollyx-pelican"
export GOLLYX_STAGE="integration"

export GOLLYX_TLD="golly456.life"
export GOLLYX_BASE_UI="https://integration.${GOLLY_TLD}"
export GOLLYX_BASE_API="https://cloud.pseudo.${GOLLY_TLD}"
export GOLLYX_MAPS_API="https://maps.${GOLLY_TLD}"
